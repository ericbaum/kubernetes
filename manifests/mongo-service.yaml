apiVersion: v1
kind: Service
metadata:
  labels:
    name: mongodb
  name: mongodb
  namespace: dojot
spec:
  clusterIP: None
  ports:
  - port: 27017
    targetPort: 27017
  selector:
    name: mongodb
